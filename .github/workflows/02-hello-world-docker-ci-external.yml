name: Test External Repo
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: cristinapetri/hello-world-docker-action@v1
        with:
          who-to-greet: '@cvesa'
